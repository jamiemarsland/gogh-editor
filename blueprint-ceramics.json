{
	"$schema": "https://playground.wordpress.net/blueprint-schema.json",
	"meta": {
		"title": "Hollowell — a ceramics studio's shop",
		"description": "A WooCommerce shop for a small ceramics studio, built with gogh: canvas pages around Woo's rails.",
		"author": "jamiemarsland",
		"categories": [
			"editor",
			"design",
			"ecommerce"
		]
	},
	"landingPage": "/?gogh-edit=1",
	"preferredVersions": {
		"php": "8.2",
		"wp": "latest"
	},
	"features": {
		"networking": true
	},
	"login": true,
	"steps": [
		{
			"step": "installTheme",
			"themeData": {
				"resource": "wordpress.org/themes",
				"slug": "twentytwentyfive"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "installPlugin",
			"pluginData": {
				"resource": "wordpress.org/plugins",
				"slug": "woocommerce"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "installPlugin",
			"pluginData": {
				"resource": "url",
				"url": "https://raw.githubusercontent.com/jamiemarsland/gogh-demo/main/gogh-playground.zip"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "runPHP",
			"code": "<?php\nrequire '/wordpress/wp-load.php';\n// blueprint.json is CDN-cached for a week wherever it's linked — so the\n// living boot code is fetched fresh from the repo (raw caches ~5 minutes).\n// If the fetch fails, fall back to whatever this cached copy knows.\n// this blueprint: the Ceramics shop design (Hollowell) with the classic header — WooCommerce is installed above\ndefine( 'GOGH_DEMO_STARTER', 'ceramics' );\ndefine( 'GOGH_DEMO_HEADER', 'gogh-header-classic' );\ndefine( 'GOGH_DEMO_TITLE', 'Hollowell' );\n$boot = false;\ntry {\n\t$boot = file_get_contents( 'https://raw.githubusercontent.com/jamiemarsland/gogh-demo/main/demo-boot.php' );\n} catch ( \\Throwable $e ) {}\nif ( $boot && false !== strpos( $boot, 'gogh' ) ) {\n\teval( '?>' . $boot );\n\treturn;\n}\n// fallback: minimal boot — apply the design with the installed plugin\nwp_set_current_user( 1 );\ntry {\n\t$req = new WP_REST_Request( 'POST', '/gogh/v1/starter' );\n\t$req->set_param( 'slug', 'ceramics' );\n\trest_do_request( $req );\n} catch ( \\Throwable $e ) {}\n"
		},
		{
			"step": "setSiteOptions",
			"options": {
				"blogname": "Hollowell",
				"blogdescription": "Stoneware from Bristol"
			}
		}
	]
}