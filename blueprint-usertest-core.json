{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "meta": {
    "title": "The core Site Editor — a user test",
    "description": "Elliot Grey’s site built in core blocks on Twenty Twenty-Five, with the same card of seven tasks the gogh test uses. Answers go to the helper under suite: core.",
    "author": "jamiemarsland",
    "categories": ["editor", "design"]
  },
  "landingPage": "/wp-admin/site-editor.php",
  "preferredVersions": { "php": "8.2", "wp": "latest" },
  "features": { "networking": true },
  "login": true,
  "steps": [
    { "step": "installTheme", "themeData": { "resource": "wordpress.org/themes", "slug": "twentytwentyfive" }, "options": { "activate": true } },
    { "step": "installPlugin", "pluginData": { "resource": "wordpress.org/plugins", "slug": "contact-form-7" }, "options": { "activate": true } },
    { "step": "installPlugin", "pluginData": { "resource": "url", "url": "https://raw.githubusercontent.com/jamiemarsland/gogh-demo/main/usertest-card.zip" }, "options": { "activate": true } },
    { "step": "runPHP", "code": "<?php\nrequire '/wordpress/wp-load.php';\nupdate_option( 'ut_card', array( 'persona' => 'photographer', 'suite' => 'core' ) );\n$boot = false;\ntry { $boot = file_get_contents( 'https://raw.githubusercontent.com/jamiemarsland/gogh-demo/main/core-boot.php' ); } catch ( \\Throwable $e ) {}\nif ( $boot && false !== strpos( $boot, 'Elliot Grey' ) ) { eval( '?>' . $boot ); }\n" }
  ]
}
